---
import Link from "@components/Link.astro";

export interface Props {
  id: number;
}

const { id } = Astro.props;
---

<Link id={`ref${id}`} href={`#note${id}`} class="align-[super] text-xs leading-none">[{id}]</Link>
