---
import Link from "./Link.astro";

export interface Props {
  id: number;
}

const { id } = Astro.props;
---

<p class="mb-3 text-sm leading-relaxed text-[color:var(--color-text-tertiary)]">
  <span id={`note${id}`}>[{id}]</span>
  <slot />
  <Link href={`#ref${id}`} class="ml-1 text-xs">↩</Link>
</p>
