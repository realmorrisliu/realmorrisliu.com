---
export interface Props {
  id: number;
}

const { id } = Astro.props;
---

<p class="mb-3 text-sm leading-relaxed text-gray-600">
  <span id={`note${id}`} class="font-medium">[{id}]</span>
  <slot />
  <a
    href={`#ref${id}`}
    class="ml-1 text-xs text-blue-600 no-underline transition-colors duration-150 hover:text-blue-800 hover:underline"
    >↩</a
  >
</p>
